# 登录鉴权（自研 JWT + BCrypt）实现计划

> **执行说明（本项目协作模式）**：后端任务（T1–T5）由用户 ice-ww **自研编写**，本计划中的代码是**参考实现**（对照编写、逐行理解，不照抄即可）；前端任务（T6）由 Claude **代写**、用户 review。**不采用 subagent 代写后端 Java**（用户偏好：SDD 代写后反馈无参与感）。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 给 ww-job 控制台加登录鉴权：admin 管理 API 全部要求 JWT 登录态，executor 机器间调用不受影响，前端加登录页 + 路由守卫 + axios 统一带 token。

**Architecture:** 新增 `sys_user` 表 + 种子 admin 账号；core 模块自研 `JwtUtil`（HMAC-SHA256 手写 header.payload.signature）；admin 新增 `AuthController`（BCrypt 校验）与 `AuthInterceptor`（拦截管理 API、放行 executor 专用路径）；前端新增 Login 页 + 路由守卫 + axios 拦截器。JWT 无状态，多 admin 用同一 secret 即可互认 token。

**Tech Stack:** Java 17、Spring Boot 3.3.5、MyBatis-Plus、BCrypt（spring-security-crypto）、Vue3 + Element Plus + Axios

**Spec:** `docs/superpowers/specs/2026-08-30-auth-jwt-design.md`

## Global Constraints

- **后端自研**：T1–T5 由用户 ice-ww 编写，计划代码为参考实现；Claude 不代写后端 Java
- **前端代写**：T6 由 Claude 编写、用户 review
- **种子密码**：`admin123`，BCrypt 固定哈希 `$2a$12$.0u57V.dCzQl3y0mIIAiZ.eELULXVyHjIqpf4YeurZvWianqnk9X.`（已生成并验证）
- **绝不在任何代码/文档中出现**真实 MySQL 密码或 QQ SMTP 授权码（见 application-local.yml）
- **executor 专用路径必须放行**：`/registry/**`、`/heartbeat`、`/callback/**`
- **401 响应体**：`{"code":401,"msg":"未登录或登录已过期"}`
- **登录失败统一提示**："用户名或密码错误"（不区分用户不存在/密码错，防枚举）
- **BCrypt 依赖**：只引 `spring-security-crypto`（不含 starter-security 全家桶）
- **JWT**：HMAC-SHA256，base64url 无填充，恒定时间比较（`MessageDigest.isEqual`）
- **jwt-secret**：默认值可提交进 application.yml（开发密钥），生产在 application-local.yml 覆盖

---

### Task 1: core 模块自研 JwtUtil（TDD）

**Files:**
- Create: `ww-job-core/src/main/java/com/wwjob/core/util/JwtUtil.java`
- Test: `ww-job-core/src/test/java/com/wwjob/core/util/JwtUtilTest.java`

**Interfaces:**
- Consumes: 无（纯 JDK：`javax.crypto.Mac`、`java.util.Base64`、`MessageDigest`）
- Produces: `JwtUtil.createToken(String username, String secret, long expireSeconds) -> String`；`JwtUtil.parseToken(String token, String secret) -> String|null`（非法/过期返回 null）

- [ ] **Step 1: 写失败测试**

```java
package com.wwjob.core.util;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class JwtUtilTest {
    static final String SECRET = "test-secret";

    @Test
    void createToken_返回三段点分结构() {
        String token = JwtUtil.createToken("admin", SECRET, 3600);
        assertNotNull(token);
        assertEquals(3, token.split("\\.").length);
    }

    @Test
    void parseToken_能取回用户名() {
        String token = JwtUtil.createToken("admin", SECRET, 3600);
        assertEquals("admin", JwtUtil.parseToken(token, SECRET));
    }

    @Test
    void parseToken_过期返回null() {
        // expireSeconds 传负数 → 签发即过期
        String token = JwtUtil.createToken("admin", SECRET, -1);
        assertNull(JwtUtil.parseToken(token, SECRET));
    }

    @Test
    void parseToken_篡改payload返回null() {
        String token = JwtUtil.createToken("admin", SECRET, 3600);
        // 把中间段 payload 的 admin 改成 eve（改后 base64url 重编码）
        String[] parts = token.split("\\.");
        String tampered = parts[0] + "." + "eyJ1c2VybmFtZSI6ImV2ZSJ9" + "." + parts[2];
        assertNull(JwtUtil.parseToken(tampered, SECRET));
    }

    @Test
    void parseToken_错误secret返回null() {
        String token = JwtUtil.createToken("admin", SECRET, 3600);
        assertNull(JwtUtil.parseToken(token, "wrong-secret"));
    }

    @Test
    void parseToken_垃圾输入返回null() {
        assertNull(JwtUtil.parseToken(null, SECRET));
        assertNull(JwtUtil.parseToken("not-a-token", SECRET));
        assertNull(JwtUtil.parseToken("a.b", SECRET)); // 只有两段
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `mvn -pl ww-job-core -am test -Dtest=JwtUtilTest`
Expected: 编译失败，`JwtUtil` 类不存在

- [ ] **Step 3: 写参考实现**

```java
package com.wwjob.core.util;

import javax.crypto.Mac;
import javax.crypto.spec.SecretKeySpec;
import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.util.Base64;

/**
 * 自研 JWT（HMAC-SHA256）。
 * JWT = base64url(header).base64url(payload).base64url(HMAC-SHA256(前两段, secret))
 */
public final class JwtUtil {
    private JwtUtil() {}

    private static final String HEADER = "{\"alg\":\"HS256\",\"typ\":\"JWT\"}";

    public static String createToken(String username, String secret, long expireSeconds) {
        long exp = System.currentTimeMillis() / 1000 + expireSeconds;
        String payload = "{\"username\":\"" + escape(username) + "\",\"exp\":" + exp + "}";
        String header = b64url(HEADER.getBytes(StandardCharsets.UTF_8));
        String body = b64url(payload.getBytes(StandardCharsets.UTF_8));
        String signingInput = header + "." + body;
        String signature = b64url(hmac(signingInput, secret));
        return signingInput + "." + signature;
    }

    /** 验签 + 过期校验，通过返回 username，否则 null。 */
    public static String parseToken(String token, String secret) {
        if (token == null) return null;
        String[] parts = token.split("\\.");
        if (parts.length != 3) return null;
        String signingInput = parts[0] + "." + parts[1];
        byte[] expect = decode(parts[2]);
        byte[] actual = hmacBytes(signingInput, secret);
        if (!MessageDigest.isEqual(expect, actual)) return null;   // 恒定时间比较，防时序侧信道
        String payload = new String(decode(parts[1]), StandardCharsets.UTF_8);
        long exp = extractExp(payload);
        if (exp <= System.currentTimeMillis() / 1000) return null; // 已过期
        return extractUsername(payload);
    }

    private static String escape(String s) {
        return s.replace("\\", "\\\\").replace("\"", "\\\"");
    }

    private static String extractUsername(String payload) {
        int i = payload.indexOf("\"username\":\"");
        if (i < 0) return null;
        int start = i + "\"username\":\"".length();
        int end = payload.indexOf('"', start);
        if (end < 0) return null;
        return payload.substring(start, end).replace("\\\"", "\"").replace("\\\\", "\\");
    }

    private static long extractExp(String payload) {
        int i = payload.indexOf("\"exp\":");
        if (i < 0) return 0;
        int start = i + "\"exp\":".length();
        int end = start;
        while (end < payload.length() && Character.isDigit(payload.charAt(end))) end++;
        if (end == start) return 0;
        return Long.parseLong(payload.substring(start, end));
    }

    private static byte[] hmacBytes(String input, String secret) {
        try {
            Mac mac = Mac.getInstance("HmacSHA256");
            mac.init(new SecretKeySpec(secret.getBytes(StandardCharsets.UTF_8), "HmacSHA256"));
            return mac.doFinal(input.getBytes(StandardCharsets.UTF_8));
        } catch (Exception e) {
            throw new IllegalStateException("HMAC-SHA256 不可用", e);
        }
    }

    private static String hmac(String input, String secret) {
        return b64url(hmacBytes(input, secret));
    }

    private static String b64url(byte[] data) {
        return Base64.getUrlEncoder().withoutPadding().encodeToString(data);
    }

    private static byte[] decode(String s) {
        return Base64.getUrlDecoder().decode(s);
    }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `mvn -pl ww-job-core -am test -Dtest=JwtUtilTest`
Expected: 全部 PASS（6 个用例）

- [ ] **Step 5: 提交**

```bash
git add ww-job-core/src/main/java/com/wwjob/core/util/JwtUtil.java ww-job-core/src/test/java/com/wwjob/core/util/JwtUtilTest.java
git commit -m "feat: core JwtUtil 自研 JWT（HMAC-SHA256，TDD）"
```

---

### Task 2: admin 依赖 + schema + 配置

**Files:**
- Modify: `ww-job-admin/pom.xml`（加 spring-security-crypto）
- Modify: `ww-job-admin/src/main/resources/db/schema.sql`（追加 sys_user 表 + 种子）
- Modify: `ww-job-admin/src/main/resources/application.yml`（加 wwjob.auth 配置）

**Interfaces:**
- Consumes: Task 1 的 `JwtUtil`
- Produces: `sys_user` 表（admin/admin123 种子）、`wwjob.auth.jwt-secret` / `wwjob.auth.jwt-expire-seconds` 配置，供 Task 4 读取

- [ ] **Step 1: admin pom 加 BCrypt 依赖**

在 `ww-job-admin/pom.xml` 的 `<dependencies>` 中、`spring-boot-starter-test` 之前追加：

```xml
<!-- BCrypt 密码哈希（版本由 Boot parent 管理） -->
<dependency>
    <groupId>org.springframework.security</groupId>
    <artifactId>spring-security-crypto</artifactId>
</dependency>
```

- [ ] **Step 2: schema.sql 追加 sys_user 表 + 种子**

在 `ww-job-admin/src/main/resources/db/schema.sql` 末尾追加（`job_alert_state` 之后）：

```sql
CREATE TABLE IF NOT EXISTS sys_user (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username   VARCHAR(64)  NOT NULL COMMENT '登录名',
    password_hash VARCHAR(100) NOT NULL COMMENT 'BCrypt 哈希',
    role       VARCHAR(32)  DEFAULT 'admin' COMMENT '角色（预留）',
    status     TINYINT      DEFAULT 1 COMMENT '1启用 0禁用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_username (username)
    ) COMMENT '控制台用户';

-- 种子管理员：admin / admin123（BCrypt 哈希固定串，幂等）
INSERT INTO sys_user (username, password_hash, role, status)
VALUES ('admin', '$2a$12$.0u57V.dCzQl3y0mIIAiZ.eELULXVyHjIqpf4YeurZvWianqnk9X.', 'admin', 1)
ON DUPLICATE KEY UPDATE username = username;
```

- [ ] **Step 3: application.yml 加 auth 配置**

在 `ww-job-admin/src/main/resources/application.yml` 末尾追加：

```yaml
wwjob:
  auth:
    jwt-secret: wwjob-dev-secret-please-override-2026   # 开发默认，生产在 application-local.yml 覆盖
    jwt-expire-seconds: 86400                            # token 有效期 24h
```

- [ ] **Step 4: 编译验证**

Run: `mvn -pl ww-job-admin -am compile`
Expected: BUILD SUCCESS（依赖解析成功）

- [ ] **Step 5: 提交**

```bash
git add ww-job-admin/pom.xml ww-job-admin/src/main/resources/db/schema.sql ww-job-admin/src/main/resources/application.yml
git commit -m "feat: admin 加 BCrypt 依赖 + sys_user 表种子 + auth 配置"
```

---

### Task 3: SysUser 实体 + Mapper

**Files:**
- Create: `ww-job-admin/src/main/java/com/wwjob/admin/entity/SysUser.java`
- Create: `ww-job-admin/src/main/java/com/wwjob/admin/mapper/SysUserMapper.java`

**Interfaces:**
- Consumes: Task 2 的 `sys_user` 表
- Produces: `SysUserMapper.selectOne(new QueryWrapper<SysUser>().eq("username", name))`，供 Task 4 登录校验

- [ ] **Step 1: 写实体**

参考 `JobInfo` 风格（`@TableName` + `@TableId` + getter/setter + 全参构造 + toString），字段对应 `sys_user` 表：

```java
package com.wwjob.admin.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;

import java.time.LocalDateTime;

/**
 * @author 王威
 * @version 1.0
 */
@TableName("sys_user")
public class SysUser {
    @TableId(type = IdType.AUTO)
    private Long id;
    private String username;
    private String passwordHash;
    private String role;
    private Integer status;
    private LocalDateTime createTime;
    private LocalDateTime updateTime;

    public SysUser() {
    }

    // getter/setter + 全参构造 + toString（对照 JobInfo 写全）
    // 注意：passwordHash 对应列 password_hash，MyBatis-Plus 驼峰映射自动处理
}
```

- [ ] **Step 2: 写 Mapper**

```java
package com.wwjob.admin.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.wwjob.admin.entity.SysUser;
import org.apache.ibatis.annotations.Mapper;

/**
 * @author 王威
 * @version 1.0
 */
@Mapper
public interface SysUserMapper extends BaseMapper<SysUser> {
}
```

- [ ] **Step 3: 编译验证**

Run: `mvn -pl ww-job-admin -am compile`
Expected: BUILD SUCCESS

- [ ] **Step 4: 提交**

```bash
git add ww-job-admin/src/main/java/com/wwjob/admin/entity/SysUser.java ww-job-admin/src/main/java/com/wwjob/admin/mapper/SysUserMapper.java
git commit -m "feat: SysUser 实体 + Mapper"
```

---

### Task 4: AuthController 登录接口

**Files:**
- Create: `ww-job-admin/src/main/java/com/wwjob/admin/controller/AuthController.java`

**Interfaces:**
- Consumes: `SysUserMapper`、`JwtUtil`、配置 `wwjob.auth.jwt-secret` / `wwjob.auth.jwt-expire-seconds`、`BCryptPasswordEncoder`
- Produces: `POST /auth/login` 返回 `ReturnT<Map<String,String>>`（data = `{token, username, role}`），供前端登录

- [ ] **Step 1: 写参考实现**

```java
package com.wwjob.admin.controller;

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import com.wwjob.admin.entity.SysUser;
import com.wwjob.admin.mapper.SysUserMapper;
import com.wwjob.core.model.ReturnT;
import com.wwjob.core.util.JwtUtil;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.HashMap;
import java.util.Map;

/**
 * @author 王威
 * @version 1.0
 */
@RestController
@RequestMapping("/auth")
public class AuthController {
    private final SysUserMapper sysUserMapper;
    private final BCryptPasswordEncoder passwordEncoder = new BCryptPasswordEncoder();

    @Value("${wwjob.auth.jwt-secret}")
    private String jwtSecret;

    @Value("${wwjob.auth.jwt-expire-seconds}")
    private long jwtExpireSeconds;

    public AuthController(SysUserMapper sysUserMapper) {
        this.sysUserMapper = sysUserMapper;
    }

    @PostMapping("/login")
    public ReturnT<Map<String, String>> login(@RequestBody LoginRequest req) {
        SysUser user = sysUserMapper.selectOne(
                new QueryWrapper<SysUser>().eq("username", req.getUsername()));
        // 用户不存在 / 禁用 / 密码错 → 统一提示，防用户名枚举
        if (user == null || user.getStatus() == null || user.getStatus() != 1
                || !passwordEncoder.matches(req.getPassword(), user.getPasswordHash())) {
            return new ReturnT<>(401, "用户名或密码错误");
        }
        String token = JwtUtil.createToken(user.getUsername(), jwtSecret, jwtExpireSeconds);
        Map<String, String> data = new HashMap<>();
        data.put("token", token);
        data.put("username", user.getUsername());
        data.put("role", user.getRole());
        return ReturnT.success(data);
    }

    /** 登录请求体 */
    public static class LoginRequest {
        private String username;
        private String password;
        public LoginRequest() {}
        public String getUsername() { return username; }
        public void setUsername(String username) { this.username = username; }
        public String getPassword() { return password; }
        public void setPassword(String password) { this.password = password; }
    }
}
```

> 参考实现要点：登录失败返回 code=401（与拦截器 401 一致），`matches` 对错误密码返回 false 天然防时序差异；`status != 1` 拒绝禁用账号。

- [ ] **Step 2: 编译 + 启动冒烟**

Run: `mvn -pl ww-job-admin -am compile`
Run: 启动 admin（local profile），`curl -X POST http://localhost:8080/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'`
Expected: `{"code":200,"msg":null,"data":{"token":"...","username":"admin","role":"admin"}}`

- [ ] **Step 3: 错误密码冒烟**

Run: `curl -X POST http://localhost:8080/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"wrong"}'`
Expected: `{"code":401,"msg":"用户名或密码错误"}`

- [ ] **Step 4: 提交**

```bash
git add ww-job-admin/src/main/java/com/wwjob/admin/controller/AuthController.java
git commit -m "feat: POST /auth/login（BCrypt 校验 + JWT 签发）"
```

---

### Task 5: AuthInterceptor + WebMvcConfig 注册

**Files:**
- Create: `ww-job-admin/src/main/java/com/wwjob/admin/interceptor/AuthInterceptor.java`
- Create: `ww-job-admin/src/main/java/com/wwjob/admin/config/WebMvcConfig.java`

**Interfaces:**
- Consumes: `JwtUtil`、配置 `jwt-secret`
- Produces: 拦截所有 `/job/**` `/joblog/**` `/jobgroup/**` `/dashboard/**`；放行 `/auth/login`、`/registry/**`、`/heartbeat`、`/callback/**`

- [ ] **Step 1: 写拦截器**

```java
package com.wwjob.admin.interceptor;

import com.wwjob.core.model.ReturnT;
import com.wwjob.core.util.JwtUtil;
import com.fasterxml.jackson.databind.ObjectMapper;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.http.MediaType;
import org.springframework.stereotype.Component;
import org.springframework.web.servlet.HandlerInterceptor;

/**
 * @author 王威
 * @version 1.0
 */
@Component
public class AuthInterceptor implements HandlerInterceptor {
    private static final String BEARER_PREFIX = "Bearer ";

    @Value("${wwjob.auth.jwt-secret}")
    private String jwtSecret;

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        String auth = request.getHeader("Authorization");
        if (auth != null && auth.startsWith(BEARER_PREFIX)) {
            String token = auth.substring(BEARER_PREFIX.length());
            String username = JwtUtil.parseToken(token, jwtSecret);
            if (username != null) {
                request.setAttribute("username", username);
                return true;
            }
        }
        response.setStatus(401);
        response.setContentType(MediaType.APPLICATION_JSON_VALUE);
        response.setCharacterEncoding("UTF-8");
        response.getWriter().write(new ObjectMapper().writeValueAsString(new ReturnT<>(401, "未登录或登录已过期")));
        return false;
    }
}
```

- [ ] **Step 2: 注册拦截器 + exclude 列表**

```java
package com.wwjob.admin.config;

import com.wwjob.admin.interceptor.AuthInterceptor;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * @author 王威
 * @version 1.0
 */
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {
    private final AuthInterceptor authInterceptor;

    public WebMvcConfig(AuthInterceptor authInterceptor) {
        this.authInterceptor = authInterceptor;
    }

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(authInterceptor)
                .addPathPatterns("/**")
                .excludePathPatterns(
                        "/auth/login",   // 登录接口本身
                        "/registry/**",  // executor 注册
                        "/heartbeat",    // executor 心跳
                        "/callback/**"   // executor 回调
                );
    }
}
```

- [ ] **Step 3: 重启 admin，验证未登录拦截**

Run: 重启 admin（local profile）
Run: `curl http://localhost:8080/job/page`
Expected: `{"code":401,"msg":"未登录或登录已过期"}`

- [ ] **Step 4: 验证 executor 路径放行 + 带 token 访问**

Run: `curl http://localhost:8080/jobgroup/list`（executor 注册前的空查询）——注意 `/jobgroup/list` 在 `/jobgroup/**` 下**会被拦截**，需带 token 或用登录返回的 token
Run: 取登录 token 后 `curl -H "Authorization: Bearer <token>" http://localhost:8080/job/page`
Expected: 200

> executor 路径专项验证放 T7（双 admin 集群端到端），此处确认 exclude 配置加载即可（`/registry/**` 等不被拦截的判断依据是集群验证时不报 401）。

- [ ] **Step 5: 提交**

```bash
git add ww-job-admin/src/main/java/com/wwjob/admin/interceptor/AuthInterceptor.java ww-job-admin/src/main/java/com/wwjob/admin/config/WebMvcConfig.java
git commit -m "feat: AuthInterceptor 拦截管理 API + 放行 executor 路径"
```

---

### Task 6: 前端登录（Claude 代写，用户 review）

**Files:**
- Create: `ww-job-web/src/views/Login.vue`
- Create: `ww-job-web/src/api/auth.js`
- Modify: `ww-job-web/src/router/index.js`（加 /login 路由 + 守卫）
- Modify: `ww-job-web/src/api/request.js`（请求带 token + 401 跳登录）
- Modify: `ww-job-web/src/App.vue`（退出登录 + 显示用户名 + login 页不套布局）

**Interfaces:**
- Consumes: `POST /auth/login` → `{code:200, data:{token, username, role}}`
- Produces: localStorage key `wwjob_token`；`/login` 路由；所有请求 `Authorization: Bearer <token>` 头

- [ ] **Step 1: 新建 auth API**（`src/api/auth.js`）

```js
import request from './request'

export function login(data) {
  return request.post('/auth/login', data)
}
```

- [ ] **Step 2: 改 router**（加登录路由 + 守卫）

```js
import Login from '../views/Login.vue'

const routes = [
  { path: '/login', component: Login },
  { path: '/', redirect: '/dashboard' },
  // ...原有路由
]

router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('wwjob_token')
  if (to.path === '/login') { next(token ? '/dashboard' : undefined); return }
  if (!token) { next('/login') } else { next() }
})
```

- [ ] **Step 3: 改 axios 拦截器**（`src/api/request.js`）

```js
request.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('wwjob_token')
    if (token) config.headers.Authorization = 'Bearer ' + token
    return config
  },
  (err) => Promise.reject(err)
)

request.interceptors.response.use(
  (res) => res,
  (err) => {
    if (err.response?.status === 401) {
      localStorage.removeItem('wwjob_token')
      window.location.href = '/login'
    }
    return Promise.reject(err)
  }
)
```

- [ ] **Step 4: 新建 Login.vue**（Element Plus 表单，全屏居中，成功后存 token 跳 /dashboard）

- [ ] **Step 5: 改 App.vue**（login 路由不渲染侧边栏布局；加用户名显示 + 退出登录）

- [ ] **Step 6: 前端自测**

Run: `cd ww-job-web && npm run dev`
验证: 访问 `/` 跳 `/login` → admin/admin123 登录 → 进 dashboard → 刷新仍登录 → 退出登录回 /login

- [ ] **Step 7: 提交**

```bash
git add ww-job-web/src/views/Login.vue ww-job-web/src/api/auth.js ww-job-web/src/router/index.js ww-job-web/src/api/request.js ww-job-web/src/App.vue
git commit -m "feat: 前端登录页 + 路由守卫 + axios 携带 token"
```

---

### Task 7: 端到端验证 + README + 推送

**Files:**
- Modify: `README.md`（功能特性 + 快速开始加登录说明）

- [ ] **Step 1: 双 admin + executor 集群回归**（executor 生命线专项）

起双 admin（8080/8082）+ executor，确认 job 27 每 5s 正常触发、job_log 持续新增、心跳/注册/回调无 401。这是 exclude 列表正确性的关键验证。

- [ ] **Step 2: 未登录/登录/错误密码/过期 token/篡改 token 五连测**（spec §10）

- [ ] **Step 3: README 补充**

功能特性加一条"控制台登录鉴权（自研 JWT + BCrypt）"；快速开始加登录说明（默认 admin/admin123，可通过 schema.sql 种子修改）。

- [ ] **Step 4: 提交 + 推送**

```bash
git add README.md
git commit -m "docs: README 补充登录鉴权说明"
git push
```
